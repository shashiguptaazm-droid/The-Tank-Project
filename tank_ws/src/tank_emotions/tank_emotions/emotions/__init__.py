"""tank_emotions.emotions — one module per emotion.

These modules are auto-imported by :func:`tank_emotions.taxonomy.discover`.
Keeping each emotion in its own file lets the catalog grow without
growing the core library.
"""
