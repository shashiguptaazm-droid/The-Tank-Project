"""Helper scripts for the Tank Project.

Modules here are standalone utilities (env loaders, pre-flight
checkers, etc.) that the ROS2 launch files and systemd units can
``import`` before starting the main nodes.
"""
