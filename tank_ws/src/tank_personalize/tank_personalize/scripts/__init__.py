"""tank_personalize.scripts — CLI-first entry points.

Per STATUS.md §9 design rule 8 (every Python module must have a
``scripts/`` wrapper for bench / CI validation), this sub-package
hosts the launcher + admin CLIs.
"""
