"""tank_learn.scripts — operator-facing CLI entry points.

Mirrors the pattern in :mod:`tank_command_bridge` (operator calls the
``python3 -m tank_learn.scripts.<x>`` form from the systemd timers).
"""
from __future__ import annotations

__all__: list[str] = []
