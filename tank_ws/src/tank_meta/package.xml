<?xml version="1.0"?>
<?xml-model
  href="http://download.ros.org/schema/package_format3.xsd"
  schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>tank_meta</name>
  <version>0.1.0</version>
  <description>
    Structured coding-agent memory layer for The Tank Project. Indexes
    every Python file (AST walk), every hardware component (static JSON),
    past decisions (decision log), and knowledge notes (markdown) into a
    single portable SQLite file, exposed to the rest of the system over
    ROS 2 std_msgs/String (JSON over topics) and to a CLI for offline use.
  </description>
  <maintainer email="tank@example.invalid">Tank Maintainer</maintainer>
  <license>MIT</license>

  <depend>rclpy</depend>
  <depend>std_msgs</depend>

  <exec_depend>python3-numpy</exec_depend>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_flake8</test_depend>
  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
