name: Sync toolkit docs to website

# Issue #38: toolkit-owned docs are authoritative for CLI / MCP / SDK /
# coding-agent setup. When they change on main, or an SDK release tag is
# created, open a PR to the privately configured documentation destination so
# the website mirror stays in sync. Website-owned docs (rest-api, cookbook,
# openapi) are excluded from the trigger so this workflow cannot create a
# sync loop.

on:
  push:
    branches:
      - main
    tags:
      - "cli-v*"
      - "mcp-v*"
      - "python-sdk-v*"
      - "js-sdk-v*"
    paths:
      # Toolkit-owned docs. The website's docs/.source-manifest.json
      # (sources.toolkit_owned.paths) is the authority for what sync-docs.mjs
      # actually mirrors; this trigger must be a SUPERSET of it so every
      # toolkit-owned documentation edit reliably starts the sync.
      - "docs/en-US/cli.md"
      - "docs/zh-CN/cli.md"
      - "docs/cn/zh-CN/cli.md"
      - "docs/en-US/mcp-server.md"
      - "docs/zh-CN/mcp-server.md"
      - "docs/cn/zh-CN/mcp-server.md"
      - "docs/en-US/ide-cli-setup.md"
      - "docs/zh-CN/ide-cli-setup.md"
      - "docs/cn/zh-CN/ide-cli-setup.md"
      - "docs/en-US/claude-code-setup.md"
      - "docs/zh-CN/claude-code-setup.md"
      - "docs/en-US/codex-setup.md"
      - "docs/zh-CN/codex-setup.md"
      - "docs/en-US/opencode-setup.md"
      - "docs/zh-CN/opencode-setup.md"
      - "docs/cn/zh-CN/opencode-setup.md"
      - "docs/en-US/getting-started.md"
      - "docs/zh-CN/getting-started.md"
      - "docs/cn/zh-CN/getting-started.md"
      # SDK docs mirrored by the website source manifest.
      - "docs/en-US/python-sdk.md"
      - "docs/zh-CN/python-sdk.md"
      - "docs/cn/zh-CN/python-sdk.md"
      - "docs/en-US/python-sdk-api.md"
      - "docs/zh-CN/python-sdk-api.md"
      - "docs/en-US/js-sdk.md"
      - "docs/zh-CN/js-sdk.md"
      - "docs/cn/zh-CN/js-sdk.md"
      - "docs/en-US/js-sdk-api.md"
      - "docs/zh-CN/js-sdk-api.md"
      - ".github/workflows/sync-docs-to-website.yml"
      - "scripts/prepare-website-docs.mjs"
      - "scripts/prepare-website-docs.test.mjs"
      - "scripts/release-tag-version.mjs"
      - "scripts/sync-website-client-versions.mjs"
      - "scripts/sync-website-client-versions.test.mjs"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sync-docs-to-website
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      WEBSITE_SYNC_TOKEN: ${{ secrets.WEBSITE_SYNC_TOKEN || secrets.REPO_ACCESS_TOKEN }}
      WEBSITE_REPO: ${{ secrets.WEBSITE_SYNC_REPOSITORY }}
      WEBSITE_BASE: ${{ secrets.WEBSITE_SYNC_BASE }}

    steps:
      - name: Check sync token
        id: guard
        run: |
          if [ -n "${WEBSITE_SYNC_TOKEN}" ] && [ -n "${WEBSITE_REPO}" ] && [ -n "${WEBSITE_BASE}" ]; then
            echo "skip=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Missing private configuration. Do NOT report a silent green
          # "success" that did nothing — that hides breakage until a manual run
          # is inspected. Do not print configured values into public logs.
          echo "::error title=Website docs sync configuration missing::Required Actions secrets are not configured. Add the sync token, destination repository, and destination base under Settings -> Secrets and variables -> Actions."
          if [ "${GITHUB_REPOSITORY}" = "QVerisAI/qveris-agent-toolkit" ]; then
            # First-party repo: a missing required secret is a misconfiguration
            # that must be visible (red), not a silent no-op.
            exit 1
          fi
          # Forks / mirrors legitimately cannot hold this secret — skip there
          # so they are not permanently red for a sync they can't perform.
          echo "Not the first-party repo (${GITHUB_REPOSITORY}); skipping fork-safe."
          echo "skip=true" >> "$GITHUB_OUTPUT"

      - name: Checkout toolkit
        if: steps.guard.outputs.skip != 'true'
        uses: actions/checkout@v7
        with:
          # Tag-triggered runs still stage non-SDK docs from current main;
          # SDK pages are pinned to their release tags below.
          ref: main
          fetch-depth: 0
          path: qveris-agent-toolkit

      - name: Record toolkit main revision
        if: steps.guard.outputs.skip != 'true'
        id: toolkit
        working-directory: qveris-agent-toolkit
        run: echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Checkout documentation destination
        if: steps.guard.outputs.skip != 'true'
        uses: actions/checkout@v7
        with:
          repository: ${{ env.WEBSITE_REPO }}
          ref: ${{ env.WEBSITE_BASE }}
          token: ${{ env.WEBSITE_SYNC_TOKEN }}
          path: website

      - name: Setup Node.js
        if: steps.guard.outputs.skip != 'true'
        uses: actions/setup-node@v7
        with:
          node-version: 20

      - name: Prepare release-aware public docs
        if: steps.guard.outputs.skip != 'true'
        id: releases
        run: >-
          node qveris-agent-toolkit/scripts/prepare-website-docs.mjs
          --toolkit-dir qveris-agent-toolkit
          --website-dir website
          --output-dir qveris-agent-toolkit-public

      - name: Mirror toolkit-owned docs into website checkout
        if: steps.guard.outputs.skip != 'true'
        working-directory: website
        run: node scripts/sync-docs.mjs --from-toolkit --toolkit-dir ../qveris-agent-toolkit-public

      - name: Synchronize published client versions
        if: steps.guard.outputs.skip != 'true'
        run: >-
          node qveris-agent-toolkit/scripts/sync-website-client-versions.mjs
          --toolkit-dir qveris-agent-toolkit
          --website-dir website

      - name: Open website PR if docs changed
        if: steps.guard.outputs.skip != 'true'
        working-directory: website
        env:
          GH_TOKEN: ${{ env.WEBSITE_SYNC_TOKEN }}
          TOOLKIT_SHA: ${{ steps.toolkit.outputs.sha }}
          CLI_TAG: ${{ steps.releases.outputs.cli_tag }}
          MCP_TAG: ${{ steps.releases.outputs.mcp_tag }}
          PYTHON_SDK_TAG: ${{ steps.releases.outputs.python_tag }}
          JS_SDK_TAG: ${{ steps.releases.outputs.js_tag }}
        run: |
          set -euo pipefail
          if [ -z "$(git status --porcelain)" ]; then
            echo "No website doc changes after sync; nothing to open."
            exit 0
          fi

          SHORT_SHA="$(echo "${TOOLKIT_SHA}" | cut -c1-12)"
          BRANCH="sync/toolkit-docs-${SHORT_SHA}"

          git config user.name "qveris-docs-sync[bot]"
          git config user.email "qveris-docs-sync@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add -A
          git commit -m "docs: sync toolkit-owned docs from qveris-agent-toolkit@${SHORT_SHA}"
          git push --force origin "${BRANCH}"

          BODY="$(cat <<EOF
          Automated mirror of **toolkit-owned** docs from \`QVerisAI/qveris-agent-toolkit\` @ \`${SHORT_SHA}\`.

          Source of truth is the toolkit repo (CLI / MCP / SDK / coding-agent setup docs). Public client version assertions are materialized from the latest published tags (\`${CLI_TAG}\`, \`${MCP_TAG}\`, \`${PYTHON_SDK_TAG}\`, \`${JS_SDK_TAG}\`); SDK pages use their tagged snapshots, while other toolkit-owned docs track toolkit main. Generated by \`.github/workflows/sync-docs-to-website.yml\` via release-aware staging and \`scripts/sync-docs.mjs --from-toolkit\`. Website-owned REST, cookbook, and OpenAPI docs are not touched by this workflow.
          EOF
          )"

          OPEN_COUNT="$(gh pr list --repo "${WEBSITE_REPO}" --head "${BRANCH}" --base "${WEBSITE_BASE}" --state open --json number --jq 'length')"
          if [ "${OPEN_COUNT}" != "0" ]; then
            echo "PR for ${BRANCH} already open; branch updated, no new PR created."
          else
            gh pr create \
              --repo "${WEBSITE_REPO}" \
              --base "${WEBSITE_BASE}" \
              --head "${BRANCH}" \
              --title "docs: sync toolkit-owned docs (qveris-agent-toolkit@${SHORT_SHA})" \
              --body "${BODY}"
          fi
